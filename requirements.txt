# Runtime dependencies for deterministic fan-out validation and the formatted
# Stage 02 Word report. Keep supported major versions bounded so a fresh ELARA
# clone uses the same APIs exercised in CI.
jsonschema>=4.18,<5
python-docx>=1.2,<2
# TOML parser for transport verification on the supported Python 3.10 host.
tomli>=2,<3; python_version < "3.11"
